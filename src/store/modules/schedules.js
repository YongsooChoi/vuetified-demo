// import Axios from "axios";
// import * as types from "@/store/mutation-types";

const state = {
  schedules: [],
};

const mutations = {};

const actions = {};

const getters = {
  schedules: (state) => state.schedules,
};

export default {
  state,
  getters,
  actions,
  mutations,
};
